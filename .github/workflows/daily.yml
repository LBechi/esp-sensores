name: Descarga diaria sensores ESP

on:
  schedule:
    - cron: '0 10 * * *'
  workflow_dispatch:

jobs:
  descargar-datos:
    runs-on: ubuntu-22.04
    permissions:
      contents: write

    steps:
      - name: Clonar repositorio
        uses: actions/checkout@v4

      - name: Instalar Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Instalar dependencias Python
        run: pip install -r requirements.txt

      - name: Instalar Chromium para Playwright
        run: playwright install --with-deps chromium

      - name: Ejecutar script de descarga
        env:
          ESP_USERNAME: ${{ secrets.ESP_USERNAME }}
          ESP_PASSWORD: ${{ secrets.ESP_PASSWORD }}
        run: python download_sensors.py

      - name: Guardar Excel en el repositorio
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add datos/
          git commit -m "Sensores $(date +'%Y-%m-%d')" || echo "Sin archivos nuevos"
          git push
